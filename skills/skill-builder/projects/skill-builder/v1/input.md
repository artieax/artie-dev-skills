# v1 — input

## Trigger

Initial creation of skill-creator as a meta-skill for artie-dev-skills.

## Goals

- Full skill lifecycle: design → scaffold → worktree iteration → evals → split → dependency registration → periodic improvement
- All-English, OSS-publishable
- Worktree-first (no `git checkout -b` in main working directory)
- `skills/` flat structure — no nested sub-skills directories
- Red flags patterns as a reusable catalog

## Branch

feature/skill-creator (worktree: artie-dev-skills-skill-creator)
